name:  CI

on:
  workflow_dispatch:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: denku
        - run:|
            echo ${{ secrets.GITHUB_TOKEN }} | gh auth login --with-token
            gh release upload latest Dockerfile
        
   
